<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:name=".DefaultApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity android:name=".update_age"></activity>
        <activity android:name=".me_config"></activity>
        <activity android:name=".add_minerals"></activity>
        <activity android:name=".add_vitamins"></activity>
        <activity android:name=".create_item"></activity>
        <activity android:name=".create_food"></activity>
        <activity android:name=".food_journal"></activity>
        <activity android:name=".AddFoodToJournal"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>